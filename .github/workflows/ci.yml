name: CI
on:
  pull_request:
  push: { branches: [main] }
jobs:
  gate:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Required checks for branch protection — Static / Security (cargo-deny) / Pentest checks / Test"
