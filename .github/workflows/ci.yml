name: CI

# Fährt alle Gates (Rust-Tests + Clippy + Lizenz-Drift, Frontend-Check +
# vitest + Playwright-e2e) bei jedem Push auf main und bei jedem Pull Request.
on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    uses: ./.github/workflows/test.yml
